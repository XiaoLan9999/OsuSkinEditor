# 更新记录

## 未发布

### Mania 设计与播放控制

- 语言入口移到菜单栏右上角，固定显示「语言 / Language」，当前语言显示勾选
- 增加独立的 Mania 流速、演示 BPM、键数、判定参考线与重置操作；使用实际经过时间推进动画
- 新增上隐遮挡、渐变、颜色、底部加高和判定位置联动的实时设计面板
- 保存为新皮肤副本，生成 StageBottom 及 KeyImage 衍生素材并只更新对应键数配置，保留原始文件及动画帧
- 处理未保存设计的切换、取消、导出失败和保存后重复应用问题；与现有 INI 保存保护配合
- 清理界面中文句号与英文句尾点号，保留文件扩展名、小数和省略号
- 137 项回归测试通过，实际导出验证覆盖 Capoo 1.5 与 Bochi 圆球 v3.0

### 小蓝头像与科技风界面

- 左上角和程序图标使用小蓝头像，背景改为深蓝电路与青色柔光；原图独立保留
- 启动欢迎区移除大头像与重复的英文品牌行，保留欢迎文字和打开皮肤入口
- 全局界面改为深蓝底、冰青选中态和电蓝主按钮，统一细描边、几何边角、线性工具图标及网格装饰
- 明确显示下拉框与数值框的方向箭头，保留禁用态、键盘焦点和滚轮保护
- 补齐冻结程序资源路径和打包资源清单，继续兼容 1030 × 680 的紧凑窗口

### Mania 预览与滚轮修复

- 数值框与键数下拉框必须先点击，才能用滚轮修改；未激活时继续滚动面板，失焦后重新锁定
- 修复首次载入总选第一个 Mania 段而显示 1K 的问题；与皮肤加载器一致优先 4K / 7K，同皮肤重载保留手动选择
- 修复按键图片从 HitPosition 开始向下绘制导致透明边距内的圆环被裁掉的问题；采用舞台底部锚点和独立的纵向 legacy 缩放
- 音符按底边定位，读取 StageHint 及其高清素材、JudgementLine 开关与颜色、KeysUnderNotes 图层顺序、各列背景色和分隔线颜色
- 移除固定粉色判定线与光晕；预览区域变窄时缩放场景，避免列被裁掉
- 使用 Capoo 1.5 副本及用户的游戏截图核对按键圆环位置；新增 24 项滚轮、键数选择和图层渲染回归测试

### 界面与操作

- 统一深色粉色主题，重做欢迎页、皮肤信息栏、素材侧栏和预览区域
- 新增素材搜索与分类、拖入皮肤目录或 `skin.ini`、预览暂停与 STD 缩放，以及更清晰的空状态
- 重做图片 / 音频管理窗口，增加搜索、文件数量、图片尺寸与音频进度信息
- 素材替换后重新载入主预览；未保存的 Mania 配置在离开当前内容前提示处理

### 文件与配置

- 改善多编码 `skin.ini`、重复 Mania 段、注释和百分号内容的读取，统一素材 `@2x` 优先级
- 编辑 Mania 配置时保留无关段、原编码与换行；保留首次 `.bak`，检测外部修改，使用临时文件完成写入
- 修复保存过程中写入未编辑字段、空字段和无效宽度的问题；改进快照及恢复流程
- 加固 `.osk` 底层导入：检查越界路径、重复名称和链接，拒绝覆盖已有文件
- 修复 `.osk` 导出包含自身或编辑器备份的问题导入 / 导出仍为底层 API

### 素材与预览

- 修复图片预览设置空文本后丢失图片的问题，并避免替换后的文件缓存导致旧图残留
- 素材列表改为只读，并将真实路径与显示文本分开保存
- 替换图片和音频前备份原文件；同名音频冲突只在同一目录内处理，不再扫描旧备份或混淆子目录
- 修复直接将 OGG / MP3 复制成 `.wav` 的编码与扩展名不一致问题；需要转换时调用 FFmpeg
- 修复初始音量与滑条显示不一致，以及关闭窗口后未释放音频的问题
- 修复 RGB 图片描边和描边透明度处理
- 改善 STD 数字前缀、覆盖层顺序与着色，以及 Mania 素材和布局的示例预览；隐藏预览时停止动画定时器
- STD 预览显示皮肤光标并读取光标居中 / 旋转设置；修复高 DPI 下的缩放与着色异常，减少透明边界计算开销

### 验证

- 新增配置、素材、压缩包、Qt 界面和预览的回归测试
- 预览仍为演示用途，不代表完整的 osu! 谱面播放或全部皮肤语义
